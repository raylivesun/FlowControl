//
// Created by admin on 14/03/24.
//

#include "nested.h"
#ifdef __cplusplus
#elif defined(__GNUC__) || defined(__GNUC__)
void nested(void)
{
    int i;
    for (i = 0; i < size_t(sizeof(int)); i++)
    {
        int j;
        for (j = 0; j < size_t(sizeof(int)); j++)
        {
            int k;
            for (k = 0; k < size_t(sizeof(int)); k++)
            {
                int l;
                for (l = 0; l < size_t(sizeof(int)); l++)
                {
                    int m;
                    for (m = 0; m < size_t(sizeof(int)); m++)
                    {
                        int n;
                        for (n = 0; n < size_t(sizeof(int)); n++)
                        {
                            return (int)(unsigned)(n *);
                        }
                    }
                }
            }
        }
    }
}
#endif//__cplusplus
